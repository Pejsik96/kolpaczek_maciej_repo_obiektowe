import java.util.Random;
public class Pull { 
    private void Pull(){
        Random rand = new Random(14);
        int i = rand.nextInt();
        switch(i){
            case 0:
        }
    }    
}
